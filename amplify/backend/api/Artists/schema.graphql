type Artist @model @key(fields: ["firstLetter", "name"]) {
  firstLetter: String!
  name: String!
  geniusId: Int!
  imageUrl: String  
  albums: [Album]
}

type Album {
  name: String!
  geniusId: Int!
  imageUrl: String!
  songs: [Song]
}

type Song {
  name: String!
  geniusId: Int!
}